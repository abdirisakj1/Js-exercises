// DOM Selector QuerySelector AND QuerySelectorAll
const title = document.querySelector('#head');
console.log(title);

const work = document.querySelector('.career');
console.log(work);
const pargraphs = document.querySelectorAll('.pargraph');
console.log(pargraphs);

const btns = document.querySelectorAll('button');

console.log(btns);